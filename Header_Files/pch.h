#pragma once

//These are headers that need to be included in each header file, so just made a separate header file with all of them in it
//to avoid needing to copy them over and over and over

#include <iostream>
#include <chrono>
#include <utility>
#include <string>
